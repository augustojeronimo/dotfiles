#!/bin/bash


PORT=$(pactl get-sink-port @DEFAULT_SINK@)

if [[ "$PORT" == *"speaker"* ]]; then
    pactl set-sink-port @DEFAULT_SINK@ analog-output-headphones
else
    pactl set-sink-port @DEFAULT_SINK@ analog-output-speaker
fi

